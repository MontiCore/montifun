/* (c) https://github.com/MontiCore/monticore */

fibonacci {

//add generic function

times2(x) = 2 * x;

private Integer fib1(Integer n) =
  if x == 0 then 0 else (
    if x == 1 then 1 else
      fib(n - 1) + fib(n - 2)
  );

public Function.A1R<Integer, Integer> fib = fib1;

}