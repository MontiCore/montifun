/* (c) https://github.com/MontiCore/monticore */
import java.nio.file.Path;
import java.lang.System;
import java.util.*;
//import java.nio.file.Paths;
//import java.lang.Math.*;

montifun javaImport {

// not side effect free
getNanoTime() = System.nanoTime();
getNanoTime(System s) = s.nanoTime();
// todo broken due to TC1 Code Bugs used in IOOSymbolsScope
//  while searching for toFile in p, it finds the toFile in this artifact instead
//  thus, renamed to toFile2
toFile2(Path p) = p.toFile();
sqrt2(double x) = java.lang.Math.sqrt(x);
char[] toCharArray2(String str) = str.toCharArray();
//todo enable after fix of
//https://git.rwth-aachen.de/monticore/monticore/-/issues/3176
//getPath(String p) = Paths.get(p);
// todo enable after https://git.rwth-aachen.de/monticore/monticore/-/issues/3319
//getPi() = PI;
//getLogOf(double a) = log(a);

}