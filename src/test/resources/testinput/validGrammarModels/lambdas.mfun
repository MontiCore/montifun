/* (c) https://github.com/MontiCore/monticore */

montifun lambdas {

Fun<Integer, Integer> times2 = (Integer arg) -> arg * 2;
times3 = arg -> arg * 3;

// todo:
// besser wäre Syntaktsiche Repräsentation des Lambda-Types, zB als:):
// (Integer -> Integer) times2 = (Integer arg) -> arg * 2;

/* todo: oder auch: 
   (int -> int) potntimes (int -> int f, int a) =
     (int b) -> if a == 1 
        then f(b) 
        else potntimes( (int x) -> f(f(x)), a-1)
   ;


*/

}